// Production build: the API is assumed to be served on the same origin as the
// SPA (so requests go to a relative `/api/...` path). Set this to the API's
// absolute URL if the backend is deployed to a separate origin.
export const environment = {
    apiUrl: ''
};
